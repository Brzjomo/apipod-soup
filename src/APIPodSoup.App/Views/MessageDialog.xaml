<Window x:Class="APIPodSoup.App.Views.MessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MessageDialog"
        Width="440" MinHeight="200" MaxHeight="480"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        MouseDown="Window_MouseDown">

    <Border Background="{StaticResource BgPrimary}"
            CornerRadius="{StaticResource CornerRadiusLg}"
            BorderBrush="{StaticResource BorderColor}"
            BorderThickness="1">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Error icon + title -->
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE783;"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="20"
                               Foreground="{StaticResource ErrorColor}"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"/>
                    <TextBlock x:Name="TitleText"
                               Text="Error"
                               FontSize="{StaticResource FontSizeLg}"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextPrimary}"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Close button -->
                <Button Grid.Column="1"
                        x:Name="CloseButton"
                        Width="32" Height="32"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="CloseButton_Click">
                    <TextBlock Text="&#xE711;"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="10"
                               Foreground="{StaticResource TextSecondary}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"/>
                </Button>
            </Grid>

            <!-- Separator -->
            <Border Grid.Row="1"
                    Height="1"
                    Background="{StaticResource BorderSubtle}"
                    Margin="0,0,0,16"/>

            <!-- Message -->
            <TextBlock Grid.Row="2"
                       x:Name="MessageText"
                       TextWrapping="Wrap"
                       FontSize="{StaticResource FontSizeBase}"
                       Foreground="{StaticResource TextSecondary}"
                       Margin="28,0,0,0"/>

            <!-- OK button -->
            <Button Grid.Row="3"
                    x:Name="OkButton"
                    Content="OK"
                    Width="80"
                    Style="{StaticResource PrimaryButtonStyle}"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0"
                    Click="OkButton_Click"/>
        </Grid>
    </Border>
</Window>
